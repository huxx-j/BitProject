<?xml version="1.0" encoding="UTF-8" ?>


<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lectureReport">

    <select id="getLectureReport" parameterType="LectureReportVo" resultType="LectureReportVo">
        <![CDATA[
        SELECT Curriculum_no, Subject, Content, Instructor, ETC, Period, Date from LectureReport where Curriculum_no=#{curriculum_no} and Date=#{date}
        ]]>
    </select>

</mapper>
